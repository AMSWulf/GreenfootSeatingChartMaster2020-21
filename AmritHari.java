// import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)


// public class AmritHari extends Student implements SpecialInterestOrHobby
// {

    // /**
     // * Constructor for the AshmitSethi class.
     // * Constructors are special methods with the same exact name as the class name.  
     // * Constructors to not have return types.
     // * Constructors can be overloaded. This means we can call a constructor with different sets of parameter
     // *  lists to initalize for different conditions (depending on what constructors have been written.
     // * @param String f (firstname)
     // * @param String l (lastname)
     // * @param int r (row of seating arrangement)
     // * @param int s (seat number within row seating arrangement)
     // * 
     // */
    // public AmritHari() {
        // portraitFile="AmritHari.png";    // Make sure to name your image files firstlast.jpg, all lowercase!!!
        // setImage(portraitFile);
    // }
    // public void act(){
      // if(Greenfoot.mouseClicked(this)){
                // getName();
                // sayName("AmritHari_seating_chart.m4a");
                // myHobby();
      // }
    // }
    // public void getName(){
        // System.out.println("My name is Amrit Hari");
    // }
    // public void myHobby() {
         // System.out.println("I like coding");
    // }
// }
